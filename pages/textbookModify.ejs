<!DOCTYPE html>
<html>
  <head>
    <title>Textbook Market</title>
    <style>
        h1   {text-align: center}
        p   {text-align: center}
        /* The navbar container */
        .topnav {
            overflow: hidden;
            background-color: #333;
        }

    /* Navbar links */
    .topnav a {
        float: left;
        display: block;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
    }

    /* Links - change color on hover */
    .topnav a:hover {
        background-color: #ddd;
        color: black;
    }
    .topnav input[type=text] {
        float: right;
        padding: 6px;
        border: none;
        margin-top: 8px;
        margin-right: 12px;
        font-size: 17px;
    }
    </style>
  </head>
  <body>
          <div class="topnav">
              <a href="/marketplace">Home</a>
              <a href="/postText">Post</a>
              <a href="/report">Report</a>
              <a href="/logout">Logout</a>
            <form id = "searchForm" action="/marketplace">
              <div class="search">
                <input id ="search-bar" type="text" name="Search" placeholder="Search for Textbook..." action="/marketplace" method="get">
              </div>
            </form> 
          </div>
        <form id="delete/modify" action="/textbookModify?textid=<%= Textbook._id %>" method="post">
          <input type="text" name="title" placeholder="<%= Textbook.title %>" value="<%= Textbook.title %>"> <br>
          <input type="text" name="author" placeholder="<%= Textbook.author %>" value="<%= Textbook.author %>"> <br>
          <input type="text" name="isbn" minlength="10" maxlength="13" placeholder="<%= Textbook.isbn %>" value="<%= Textbook.isbn %>"> <br>
          <input type="number" id="price" name="price" step="0.01" min="0.00" maxlength="10" placeholder="<%= Textbook.price %>" value="<%= Textbook.price %>"> <br>
          <select name="make">
                  <option value="<%= Textbook.make %>"><%= Textbook.make.charAt(0).toUpperCase() + Textbook.make.substr(1) %></option>
                  <option value="Not Specified">Not Specified</option>
                  <option value="hardcover">Hardcover</option>
                  <option value="paperback">Paperback</option>
                </select> <br><br>
            <input type="submit" name="submit" value="Delete" onclick="return confirm('Are you sure you want to delete this item?')" action="/profile"> <br>
            <input type="submit" name="submit" value="Modify" onclick="return confirm('Are yo sure you want to modify this item?')">
          </form>

  </body>
</html>
